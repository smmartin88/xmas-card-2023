import Image from 'next/image'
import Tabs from '../components/Tabs'

// TODO: add a grid of pictures to home page, have a card with a greeting within the grid, add a fade out to the footer

export default function Home() {
  return (
    <div>
      <Tabs/>
    </div>
  )
}
